# This repository tracks ONLY the BetterCR extension — the improved rewrite
# of BetterCrunchyroll. Every other file/folder stays on disk locally but is
# untracked (hidden from git/GitHub).
/*
!/.gitignore
!/BetterCR/
